/**
 * @whereq/react — WhereQ's open-source React component library.
 *
 * Public entry point. Components are exported flat for easy tree-shaking:
 *
 * ```ts
 * import { Scrollbar, GlobalScrollbar } from '@whereq/react'
 * ```
 */
export * from './scrollbar'
